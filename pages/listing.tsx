import * as React from 'react'
import { Layout } from '@components/Layout'
import Head from 'next/head'

const ListingPage: React.FunctionComponent = props => (
  <Layout>
    <Head>
      <title>Listing</title>
    </Head>
    <div>Listing</div>
  </Layout>
)

export default ListingPage
